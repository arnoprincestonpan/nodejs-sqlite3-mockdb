initialize:
===========

npm init

npm install sqlite3

npm audit fix

start:
=====

create index.js
add...

const sqlite3 = require("sqlite3").verbose()

create mock.db
back to index.js...

const db = new sqlite3.Database('./mock.db', sqlite3.OPEN_READWRITE, (err) => {
    if(err) return console.error(err.message)

    console.log("connection successful")
})

db.run("CREATE TABLE users(first_name, last_name, user_name, password)")

db.close((err) =>{
    if(err) return console.error(err.message)
})
